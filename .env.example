# LEGACY — these vars are only relevant if main.py's Railway/aiogram bot is
# ever redeployed somewhere. Railway itself has been decommissioned; the
# live project (scraper -> olx-free-stuff-worker Cloudflare Worker -> this
# repo's data/offers.json) doesn't use this file. See README.md.

BOT_TOKEN=your_bot_token_here
WEBHOOK_SECRET=change_me
PORT=8080
ADMIN_USER_ID=0
GITHUB_PAT=
MYMEMORY_EMAIL=bot@olxfreestuff.local
GITHUB_REPO=alexalkor/olx-free-stuff-bot
